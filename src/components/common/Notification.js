import React from 'react';

const Notification = () => {
  // This component can be enhanced to show global notifications
  // For now, it's a placeholder that can be used with a notification context
  return null;
};

export default Notification;